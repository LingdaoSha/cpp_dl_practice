# C++ Deep Learning Practice
practice of using cpp for deep learning

## Run
Run the following command in terminal will start a docker container with `ubuntu:18.04` and `g++` environment.

``` bash
$ bash start_docker.sh
```
